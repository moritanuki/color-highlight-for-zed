{
  "name": "color-highlight-for-zed",
  "version": "0.1.0",
  "private": true,
  "description": "VS Code-style color chips for Zed via LSP document colors.",
  "scripts": {
    "build:extension": "node scripts/build-extension.cjs",
    "check": "node --check server/color-highlight-server.cjs && node --check scripts/build-extension.cjs && node --check scripts/source-fingerprint.cjs && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
